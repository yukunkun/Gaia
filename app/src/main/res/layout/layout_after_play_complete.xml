<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
             android:id="@+id/replay_wrapper"
             android:layout_width="match_parent"
             android:layout_height="match_parent"
             android:orientation="vertical">

    <ImageView
        android:id="@+id/ic_replay"
        android:layout_width="@dimen/replay_ic_width"
        android:layout_height="@dimen/replay_ic_width"
        android:layout_gravity="center"
        android:background="@drawable/vector_drawable_ic_replay_white___px"/>
</FrameLayout>